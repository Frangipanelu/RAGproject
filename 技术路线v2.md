# 技术路线 v2

> **Workspace:** `d:\A-pythonProject\RAGProject`  
> **文档目标：** 记录当前个人知识库 / RAG 项目的实际技术路线、模块职责、运行流程、数据结构和后续演进方向。

---

# 📊 RAG 项目当前技术路线分析

## 一、项目整体架构

```
┌─────────────────────────────────────────────────────────────────┐
│                    前端展示层 (Streamlit)                         │
│                         app_main.py                              │
│  工作区管理 / 分类树 / 文件上传 / 文档列表 / 标签筛选 / AI 对话     │
└──────────────────────────────┬──────────────────────────────────┘
                               │
                               ▼
┌─────────────────────────────────────────────────────────────────┐
│                    业务逻辑层 (Core Services)                     │
├──────────────────────┬──────────────────────┬───────────────────┤
│ workspace_manager.py │ knowledge_base.py    │ rag.py            │
│ 工作区/分类/树形结构  │ 文档入库/去重/标签    │ RAG 检索问答链      │
├──────────────────────┼──────────────────────┼───────────────────┤
│ vector_stores.py     │ history.py           │ answer_saver.py   │
│ Chroma 检索器封装     │ 对话历史文件存储      │ 问答沉淀为 Markdown │
└──────────────────────┴──────────────────────┴───────────────────┘
                               │
                               ▼
┌─────────────────────────────────────────────────────────────────┐
│                       数据存储层                                  │
│  workspace_db.json        # 工作区与分类 JSON 数据库               │
│  data/knowledge_meta.json # 文档元数据 JSON 数据库                  │
│  data/md5.text            # 文档内容 MD5 去重记录                   │
│  data/uploads/            # 原始上传文件                            │
│  data/chroma_db/          # Chroma 向量数据库                       │
│  data/chat_history/       # LangChain 对话历史                      │
└─────────────────────────────────────────────────────────────────┘
                               │
                               ▼
┌─────────────────────────────────────────────────────────────────┐
│                    模型与外部服务层                                │
│  SiliconFlow OpenAI-Compatible API                              │
│  Embedding: BAAI/bge-large-zh-v1.5                              │
│  Chat: nex-agi/Nex-N2-Pro                                       │
└─────────────────────────────────────────────────────────────────┘
```

---

## 二、核心文件职责说明

| 文件 / 模块 | 职责 | 核心功能 |
|---|---|---|
| `app_main.py` | Streamlit 前端入口 | 页面布局、低饱和主题样式、工作区/分类管理、文件上传、文档列表、标签筛选、AI 对话 |
| `config_data.py` | 全局配置 | 文件路径、Chroma 配置、模型配置、SiliconFlow API 配置、Embedding 客户端创建 |
| `core/knowledge_base.py` | 知识库服务 | 文档解析、MD5 去重、自动分类、标签提取、文本切分、Chroma 入库、元数据维护 |
| `core/vector_stores.py` | 向量检索服务 | 基于 Chroma 构建检索器，支持按工作区、分类过滤 |
| `core/rag.py` | RAG 问答服务 | 构建 LangChain 检索增强生成链，调用 Chat 模型生成回答 |
| `core/workspace_manager.py` | 工作区与分类管理 | 工作区 CRUD、多级分类、分类树构建、路径同步、级联删除 |
| `core/history.py` | 对话历史管理 | 基于 JSON 文件持久化 LangChain 多轮对话历史 |
| `core/answer_saver.py` | 问答沉淀工具 | 将问答内容整理为 Markdown，并可重新入库 |
| `workspace_db.json` | 工作区数据库 | 保存工作区与分类信息 |
| `data/knowledge_meta.json` | 文档元数据数据库 | 保存文档 ID、文件名、分类、标签、块数量、上传时间等 |
| `data/md5.text` | 去重记录 | 保存已入库文档内容 MD5 |
| `data/chroma_db/` | 向量数据库 | 保存文档向量、索引和 Chroma 元数据 |
| `data/uploads/` | 原始文件目录 | 按文件类型保存用户上传的原始文件 |
| `data/chat_history/` | 对话历史目录 | 保存 `user_001` 等会话的 JSON 消息历史 |

---

## 三、实现路径详解

### 📝 路径 1：应用启动与页面初始化

```
启动 Streamlit
    → app_main.py 执行
    → 加载全局 CSS 样式
    → 初始化 session_state
    → 读取 workspace_db.json
    → 渲染顶部 Hero、侧边栏、工作区、分类树、主区域 Tabs
```

**关键初始化内容：**

1. **页面配置**：
   - `page_title="个人知识库"`
   - `layout="wide"`
   - `initial_sidebar_state="expanded"`

2. **Session 状态**：
   - `current_ws`：当前工作区 ID
   - `current_cat`：当前分类 ID
   - `current_doc`：当前查看文档 ID
   - `active_tag`：当前标签筛选
   - `show_new_note`：是否显示新建笔记表单
   - `chat_msgs`：聊天界面消息列表
   - `tree_expanded`：分类树展开状态

3. **页面结构**：
   - 顶部 Hero：项目标题、副标题、当前工作区统计
   - 左侧 Sidebar：工作区选择、分类树、标签云、工作区管理
   - 主区域 Tabs：
     - `📁 文件管理`
     - `💬 AI 对话`

---

### 📝 路径 2：工作区与分类管理

```
创建/读取工作区
    → workspace_manager.py
    → workspace_db.json
    → 分类树 / 分类路径 / 级联删除
```

当前项目使用 **JSON 文件** 作为工作区与分类数据库，而不是传统关系型数据库。

#### 工作区结构

```json
{
  "workspaces": {
    "ws_xxx": {
      "id": "ws_xxx",
      "name": "技术学习",
      "description": "",
      "icon": "💼",
      "created_at": "2026-06-26 22:31:29"
    }
  },
  "categories": {
    "cat_xxx": {
      "id": "cat_xxx",
      "name": "RAG",
      "parent_id": "cat_parent",
      "workspace_id": "ws_xxx",
      "path": "AI/RAG",
      "level": 1,
      "created_at": "2026-06-26 22:36:01"
    }
  }
}
```

#### 分类管理能力

| 能力 | 说明 |
|---|---|
| 创建顶级分类 | 父级为 `None`，路径等于分类名 |
| 创建子分类 | 继承父级路径，例如 `AI/RAG/Embedding` |
| 分类树渲染 | 递归构建树形结构，支持展开/折叠 |
| 重命名分类 | 同步更新后代分类路径 |
| 移动分类 | 防止移动到自己后代下，避免循环引用 |
| 删除分类 | 级联删除子分类 |
| 工作区删除 | 删除该工作区下所有分类 |

---

### 📝 路径 3：文档上传与知识库构建

```
用户上传文件
    → app_main.py 解析文件
    → extract_text()
    → KnowledgeBaseService.upload_by_str()
    → MD5 去重
    → 自动分类 / 标签提取
    → 文本切分
    → 向量化
    → Chroma 入库
    → 更新 data/knowledge_meta.json
```

#### 支持文件格式

当前 `ALLOWED_EXTS` 支持：

```text
md, txt, pdf, docx, html, csv, json, yaml, yml
```

#### 文本提取方式

| 文件类型 | 处理方式 |
|---|---|
| `md / txt / yaml / yml` | 直接按 UTF-8 解码 |
| `pdf` | 使用 `pypdf.PdfReader` 提取页面文本 |
| `docx` | 使用 `python-docx.Document` 提取段落文本 |
| `html` | 使用 `HTMLParser` 去除 `script/style` 后提取文本 |
| `csv` | 使用 `csv.reader` 按行拼接为 ` | ` 分隔文本 |
| `json` | 格式化 JSON 后入库 |

#### MD5 去重机制

```python
md5_hex = get_string_md5(data)

if check_md5(md5_hex):
    return {"status": "skipped", "message": "内容已存在知识库中"}
```

去重记录保存在：

```text
data/md5.text
```

这样可以避免重复上传相同内容，减少向量库冗余。

#### 自动分类

当前使用 **关键词规则分类**，配置在 `config_data.py`：

```python
AUTO_CATEGORIES = {
    "技术": ["代码", "编程", "算法", "框架", "API", "数据库", "服务器", "部署", "架构", "Python", "Java", "JavaScript", "SQL", "Git", "Docker", "Linux", "前端", "后端", "开发", "技术栈"],
    "工作": ["面试", "简历", "JD", "岗位", "项目", "团队", "管理", "绩效", "OKR", "KPI", "汇报", "协作", "沟通", "领导", "职业规划"],
    "学习": ["课程", "笔记", "读书", "学习", "考试", "培训", "教程", "知识", "概念", "理论", "原理", "方法论"],
    "生活": ["健康", "运动", "饮食", "旅行", "购物", "理财", "投资", "保险", "住房", "装修", "生活"],
    "个人": ["日记", "随笔", "想法", "感悟", "反思", "总结", "计划", "目标", "习惯"],
}
```

如果文本没有命中任何规则，则使用：

```python
DEFAULT_CATEGORY = "未分类"
```

#### 标签提取

当前支持两种策略：

1. **LLM 标签提取**：调用 Chat 模型提取核心标签。
2. **jieba 关键词提取**：当 LLM 提取失败时降级使用 `jieba.analyse.extract_tags`。

标签处理流程：

```text
文本
    → jieba 分词 / TF-IDF 关键词
    → 过滤停用词
    → 过滤过短、过长、纯数字、泛化词
    → 去重
    → 返回前 N 个标签
```

停用词表位于 `core/knowledge_base.py`，用于减少“的、了、在、是、我们、相关”等低价值词进入标签系统。

#### 文本切分

当前配置在 `config_data.py`：

| 参数 | 值 | 说明 |
|---|---:|---|
| `chunk_size` | 500 | 文本切分块大小 |
| `chunk_overlap` | 50 | 切分块重叠字符数 |
| `separators` | `\n\n`, `\n`, `。`, `！`, `？`, `.`, `!`, `?`, ` `, `` | 切分优先级 |
| `max_split_char_number` | 500 | 超过该长度才主动切分 |
| `max_embedding_chars` | 1500 | Embedding 单次最大字符保护阈值 |

切分逻辑：

```python
if len(data) > config.max_split_char_number:
    chunks = self.splitter.split_text(data)
else:
    chunks = [data]
```

同时增加二次保护：如果单个 chunk 超过 `max_embedding_chars`，会强制继续切分，避免超过 Embedding 模型输入限制。

#### Chroma 入库

入库元数据包括：

```python
{
    "doc_id": doc_id,
    "source": filename,
    "workspace_id": workspace_id,
    "category_id": category_id,
    "category_path": category_path,
    "category": category,
    "tags": json.dumps(tags, ensure_ascii=False),
    "level": hierarchy_level,
    "headings": json.dumps(headings, ensure_ascii=False),
    "create_time": upload_time,
    "chunk_count": len(chunks),
    "source_type": source_type,
}
```

向量库路径：

```text
data/chroma_db/
```

文档元数据路径：

```text
data/knowledge_meta.json
```

---

### 🔍 路径 4：检索问答流程

```
用户提问
    → RagService.chain.invoke()
    → VectorStoreService.get_retriever()
    → Chroma 向量检索
    → 格式化参考资料
    → 构建 Prompt
    → Chat 模型生成回答
    → 返回 Streamlit 聊天界面
```

#### RAG 链结构

`core/rag.py` 中构建 LangChain 链：

```python
chain = (
    {
        "input": RunnablePassthrough(),
        "context": RunnableLambda(format_for_retriever)
                   | RunnableLambda(retrieve_with_retry)
                   | format_document
    }
    | RunnableLambda(format_for_prompt_template)
    | self.prompt_template
    | print_prompt
    | self.chat_model
    | StrOutputParser()
)
```

#### 多轮对话历史

使用：

```python
RunnableWithMessageHistory(
    chain,
    get_history,
    input_messages_key="input",
    history_messages_key="history",
)
```

历史会话保存在：

```text
data/chat_history/user_001
```

`session_id` 配置在 `config_data.py`：

```python
session_config = {
    "configurable": {
        "session_id": "user_001",
    }
}
```

#### 系统提示词

当前系统提示词强调：

1. 严格基于知识库参考资料回答。
2. 不编造信息。
3. 如果没有相关资料，明确告知用户。
4. 回答时注明来源文件名。
5. 回答简洁、专业、有条理。

#### 检索过滤

`VectorStoreService.get_retriever()` 支持：

| 过滤条件 | 说明 |
|---|---|
| `workspace_id` | 限定当前工作区 |
| `category_id` | 限定当前分类 |
| `category` | 兼容旧版分类字符串过滤 |

多个条件组合时使用 Chroma `$and` 过滤器。

---

### 🗂️ 路径 5：数据存储架构

#### 1. 工作区与分类数据库

```text
workspace_db.json
```

用途：

- 保存工作区基本信息。
- 保存多级分类结构。
- 保存分类路径、层级、父子关系。

#### 2. 文档元数据数据库

```text
data/knowledge_meta.json
```

用途：

- 保存文档 ID。
- 保存文件名。
- 保存工作区、分类、标签。
- 保存上传时间、块数量、MD5、来源类型。

#### 3. 去重记录

```text
data/md5.text
```

用途：

- 每行一个 MD5。
- 用于判断文档内容是否已经入库。

#### 4. 原始上传文件

```text
data/uploads/
├── md/
├── txt/
├── pdf/
├── docx/
├── html/
├── csv/
├── json/
├── yaml/
└── yml/
```

用途：

- 保存用户上传的原始文件。
- 按扩展名分目录存储。
- 同名文件自动追加序号。

#### 5. Chroma 向量数据库

```text
data/chroma_db/
├── chroma.sqlite3
└── <collection_id>/
    ├── data_level0.bin
    ├── header.bin
    ├── length.bin
    └── link_lists.bin
```

用途：

- 保存文档向量。
- 保存 Chroma 元数据索引。
- 支撑相似度检索。

#### 6. 对话历史

```text
data/chat_history/
└── user_001
```

用途：

- 保存多轮对话消息。
- 支持 RAG 问答上下文延续。

---

## 四、技术栈依赖关系

```text
app_main.py
    ├── streamlit                    # UI 框架
    ├── python-docx                  # DOCX 解析
    ├── pypdf                        # PDF 解析
    ├── jieba                        # 中文分词与关键词提取
    ├── config_data.py               # 全局配置
    ├── core.knowledge_base          # 知识库服务
    ├── core.rag                     # RAG 问答服务
    └── core.workspace_manager       # 工作区与分类管理
            │
            ▼
core.knowledge_base.py
    ├── langchain_chroma.Chroma       # Chroma 向量存储
    ├── langchain_text_splitters      # 文本切分
    ├── langchain_openai.ChatOpenAI   # LLM 标签提取
    ├── jieba.analyse                 # 中文关键词提取
    └── json / hashlib / pathlib      # 元数据、MD5、路径处理
            │
            ▼
core.rag.py
    ├── langchain_core.runnables      # LCEL 链式编排
    ├── langchain_core.prompts        # Prompt 模板
    ├── langchain_openai.ChatOpenAI   # 对话模型
    ├── core.vector_stores            # 向量检索器
    ├── core.history                  # 对话历史
    └── StrOutputParser               # 输出解析
            │
            ▼
core.vector_stores.py
    └── Chroma.as_retriever()         # 相似度检索器
```

---

## 五、执行流程图

### 文档上传流程

```
┌─────────────────────────────────────────────────────────────────────┐
│                         文档上传流程                                  │
├─────────────────────────────────────────────────────────────────────┤
│  [用户]                                                             │
│    │                                                                │
│    ▼                                                                │
│  [Streamlit 文件上传组件]                                            │
│    │                                                                │
│    ▼                                                                │
│  [extract_text()] 解析 md/pdf/docx/html/csv/json/yaml/txt            │
│    │                                                                │
│    ▼                                                                │
│  [KnowledgeBaseService.upload_by_str()]                              │
│    │                                                                │
│    ├──→ [MD5 去重检查] → 已存在 → 返回 skipped                       │
│    │                                                                │
│    ├──→ [自动分类] → 关键词规则匹配                                  │
│    │                                                                │
│    ├──→ [标签提取] → LLM 优先，失败降级 jieba                         │
│    │                                                                │
│    ├──→ [文本切分] → RecursiveCharacterTextSplitter                  │
│    │                                                                │
│    ├──→ [向量化] → BAAI/bge-large-zh-v1.5                            │
│    │                                                                │
│    └──→ [Chroma.add_texts()]                                         │
│              │                                                      │
│              ▼                                                      │
│        [更新 knowledge_meta.json]                                    │
│        [追加 data/md5.text]                                          │
└─────────────────────────────────────────────────────────────────────┘
```

### 问答流程

```
┌─────────────────────────────────────────────────────────────────────┐
│                          问答流程                                    │
├─────────────────────────────────────────────────────────────────────┤
│  [用户提问]                                                         │
│    │                                                                │
│    ▼                                                                │
│  [RagService.chain.invoke({"input": prompt})]                       │
│    │                                                                │
│    ├──→ [RunnableWithMessageHistory] 读取 data/chat_history/user_001 │
│    │                                                                │
│    ├──→ [VectorStoreService.get_retriever()]                        │
│    │        │                                                       │
│    │        ├── workspace_id 过滤                                    │
│    │        └── category_id 过滤                                     │
│    │                                                                │
│    ├──→ [Chroma 相似度检索]                                         │
│    │        │                                                       │
│    │        └── top_k = 5                                           │
│    │                                                                │
│    ├──→ [format_document()] 格式化来源与正文                          │
│    │                                                                │
│    ├──→ [ChatPromptTemplate] 构建 Prompt                            │
│    │                                                                │
│    ├──→ [ChatOpenAI / Nex-N2-Pro] 生成回答                           │
│    │                                                                │
│    └──→ [StrOutputParser()] 返回文本                                │
│              │                                                      │
│              ▼                                                      │
│        [Streamlit 聊天界面展示]                                      │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 六、当前关键配置参数

| 参数 | 当前值 | 说明 |
|---|---:|---|
| `collection_name` | `personal_kb` | Chroma 集合名称 |
| `persist_directory` | `./data/chroma_db` | Chroma 持久化目录 |
| `chunk_size` | 500 | 文本切分块大小 |
| `chunk_overlap` | 50 | 切分块重叠字符数 |
| `max_split_char_number` | 500 | 超过后主动切分 |
| `max_embedding_chars` | 1500 | Embedding 输入保护阈值 |
| `retrieve_top_k` | 5 | 每次检索返回文档数 |
| `embedding_model_name` | `BAAI/bge-large-zh-v1.5` | 中文 Embedding 模型 |
| `chat_model_name` | `nex-agi/Nex-N2-Pro` | 对话生成模型 |
| `session_id` | `user_001` | 当前对话历史会话 ID |

---

## 七、关键设计亮点

1. **JSON 文件型轻量数据库**
   - 不依赖 MySQL、PostgreSQL 等外部数据库。
   - 适合个人知识库、本地原型和轻量部署。

2. **工作区 + 多级分类**
   - 支持多工作区隔离知识。
   - 支持分类树、分类路径、子分类、级联删除。

3. **文档 MD5 去重**
   - 避免重复上传相同内容。
   - 减少 Chroma 向量库冗余。

4. **多格式文档解析**
   - 支持 Markdown、TXT、PDF、DOCX、HTML、CSV、JSON、YAML 等常见格式。

5. **自动分类与标签提取**
   - 自动分类使用关键词规则，稳定可控。
   - 标签提取优先 LLM，失败时降级到 jieba。

6. **RAG 检索增强生成**
   - 基于 Chroma 向量检索。
   - 支持按工作区、分类过滤。
   - 回答要求基于知识库资料，减少幻觉。

7. **多轮对话历史**
   - 使用 LangChain `RunnableWithMessageHistory`。
   - 对话历史持久化到本地 JSON 文件。

8. **低饱和 Streamlit UI**
   - 当前界面采用暖灰、米白、低饱和棕色主按钮。
   - 上传区、聊天气泡、分类树、侧边栏均做卡片化与层次化处理。

---

## 八、当前局限与后续优化方向

### 当前局限

1. **工作区数据库是 JSON 文件**
   - 适合轻量使用。
   - 如果多人并发写入，需要迁移到 SQLite/PostgreSQL。

2. **自动分类依赖关键词规则**
   - 当前分类稳定但扩展性有限。
   - 后续可改为 LLM 分类或可配置分类模型。

3. **标签提取依赖外部模型**
   - LLM 提取更准确，但依赖 API。
   - 当前已有 jieba 降级方案。

4. **没有用户权限体系**
   - 当前是单用户本地知识库。
   - 如果多人使用，需要增加登录、权限、工作区共享机制。

5. **回答保存功能已有工具模块但未完全接入 UI**
   - `core/answer_saver.py` 已支持将问答整理成 Markdown。
   - 后续可以在聊天界面增加“保存到知识库”按钮。

6. **引用来源展示仍可增强**
   - 当前回答要求注明来源。
   - 后续可增加引用编号、来源卡片、点击跳转原文。

### 后续优化方向

| 方向 | 说明 |
|---|---|
| 数据库升级 | 将 `workspace_db.json`、`knowledge_meta.json` 迁移到 SQLite |
| 分类升级 | 使用 LLM 自动分类 + 用户确认机制 |
| 标签升级 | 支持标签合并、别名、同义词、标签规范 |
| 引用增强 | 回答中增加 `[1]` 引用编号，点击可展开来源 |
| 问答沉淀 | 在聊天界面增加“保存回答到知识库” |
| 权限体系 | 增加用户登录、工作区权限、分享链接 |
| 搜索增强 | 增加全文搜索、标签搜索、分类搜索组合过滤 |
| 部署优化 | 支持 Docker、后台服务、Nginx 反向代理 |

---

## 九、启动方式

```bash
cd d:\A-pythonProject\RAGProject
.venv\Scripts\python.exe -m streamlit run app_main.py --server.address localhost --server.port 8501
```

或使用 `uv`：

```bash
cd d:\A-pythonProject\RAGProject
uv run streamlit run app_main.py --server.address localhost --server.port 8501
```

启动后访问：

```text
http://localhost:8501
```

---

## 十、当前项目运行依赖说明

当前 `.venv` 中已安装并验证可用：

```text
streamlit==1.58.0
langchain-core==1.4.8
langchain-openai==1.3.3
langchain-chroma==1.1.0
chromadb==1.5.9
pypdf==6.14.2
python-docx==1.2.0
jieba==0.42.1
python-dotenv==1.2.2
opentelemetry-api==1.43.0
protobuf==5.29.5
```

如果使用全新虚拟环境，建议同时安装 `pyproject.toml` 中的依赖和当前项目实际使用的额外依赖：

```bash
uv pip install -r requirements.txt
uv pip install jieba python-dotenv protobuf
```

---

# 总结

当前项目已经形成一个完整的个人知识库 RAG 闭环：

```text
文档上传 → 文本解析 → 自动分类 → 标签提取 → 向量入库
                                      ↓
用户提问 → 工作区/分类过滤检索 → Prompt 构建 → LLM 回答 → 多轮对话历史
```

项目定位是 **本地优先、轻量部署、个人知识管理、RAG 智能问答**。当前架构适合快速迭代和个人使用，后续如果需要多人协作或生产部署，可以逐步将 JSON 存储升级为 SQLite/PostgreSQL，并补充权限、引用、审计和部署能力。
