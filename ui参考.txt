<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Knowledge Base - Personal Notes</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=JetBrains+Mono:wght@400;500&display=swap" rel="stylesheet">
    <style>
        :root {
            --paper-bg: #FAF9F6;
            --paper-line: #E8E4DF;
            --ink-primary: #2C2C2C;
            --ink-secondary: #6B6B6B;
            --ink-tertiary: #A8A8A8;
            --accent: #2563EB;
            --accent-light: #DBEAFE;
            --accent-hover: #1D4ED8;
            --sidebar-bg: #F5F4F0;
            --card-bg: #FFFFFF;
            --border: #E5E2DD;
            --success: #10B981;
            --warning: #F59E0B;
            --danger: #EF4444;
            --shadow-sm: 0 1px 2px rgba(0,0,0,0.04);
            --shadow-md: 0 4px 12px rgba(0,0,0,0.06);
            --shadow-lg: 0 8px 30px rgba(0,0,0,0.08);
            --radius: 8px;
            --radius-lg: 12px;
            --transition: all 0.2s ease;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
            background: var(--paper-bg);
            color: var(--ink-primary);
            line-height: 1.7;
            overflow: hidden;
            height: 100vh;
        }

        /* ========== 三栏布局 ========== */
        .app-container {
            display: grid;
            grid-template-columns: 280px 1fr 320px;
            height: 100vh;
            overflow: hidden;
        }

        /* ========== 左侧边栏 ========== */
        .sidebar {
            background: var(--sidebar-bg);
            border-right: 1px solid var(--border);
            display: flex;
            flex-direction: column;
            overflow: hidden;
        }

        .sidebar-header {
            padding: 20px;
            border-bottom: 1px solid var(--border);
        }

        .logo {
            font-size: 18px;
            font-weight: 700;
            color: var(--ink-primary);
            display: flex;
            align-items: center;
            gap: 10px;
            margin-bottom: 16px;
        }

        .logo-icon {
            width: 32px;
            height: 32px;
            background: var(--accent);
            border-radius: 8px;
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-size: 16px;
        }

        .search-box {
            position: relative;
        }

        .search-box input {
            width: 100%;
            padding: 10px 14px 10px 36px;
            border: 1px solid var(--border);
            border-radius: var(--radius);
            background: var(--card-bg);
            font-size: 14px;
            color: var(--ink-primary);
            transition: var(--transition);
        }

        .search-box input:focus {
            outline: none;
            border-color: var(--accent);
            box-shadow: 0 0 0 3px var(--accent-light);
        }

        .search-box input::placeholder {
            color: var(--ink-tertiary);
        }

        .search-icon {
            position: absolute;
            left: 12px;
            top: 50%;
            transform: translateY(-50%);
            color: var(--ink-tertiary);
            font-size: 14px;
        }

        .quick-capture-btn {
            margin-top: 12px;
            width: 100%;
            padding: 10px;
            background: var(--accent);
            color: white;
            border: none;
            border-radius: var(--radius);
            font-size: 14px;
            font-weight: 500;
            cursor: pointer;
            transition: var(--transition);
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 6px;
        }

        .quick-capture-btn:hover {
            background: var(--accent-hover);
        }

        .sidebar-content {
            flex: 1;
            overflow-y: auto;
            padding: 16px 12px;
        }

        .sidebar-section {
            margin-bottom: 20px;
        }

        .sidebar-section-title {
            font-size: 11px;
            font-weight: 600;
            color: var(--ink-tertiary);
            text-transform: uppercase;
            letter-spacing: 0.5px;
            padding: 0 8px;
            margin-bottom: 8px;
        }

        .nav-item {
            display: flex;
            align-items: center;
            gap: 10px;
            padding: 8px 12px;
            border-radius: var(--radius);
            cursor: pointer;
            transition: var(--transition);
            font-size: 14px;
            color: var(--ink-secondary);
            text-decoration: none;
        }

        .nav-item:hover {
            background: rgba(0,0,0,0.03);
            color: var(--ink-primary);
        }

        .nav-item.active {
            background: var(--accent-light);
            color: var(--accent);
            font-weight: 500;
        }

        .nav-item .badge {
            margin-left: auto;
            font-size: 11px;
            padding: 2px 8px;
            background: var(--border);
            border-radius: 10px;
            color: var(--ink-tertiary);
        }

        /* 文件树 */
        .file-tree {
            padding-left: 4px;
        }

        .tree-item {
            display: flex;
            align-items: center;
            gap: 8px;
            padding: 6px 8px;
            border-radius: var(--radius);
            cursor: pointer;
            transition: var(--transition);
            font-size: 14px;
            color: var(--ink-secondary);
        }

        .tree-item:hover {
            background: rgba(0,0,0,0.03);
            color: var(--ink-primary);
        }

        .tree-item.active {
            background: var(--accent-light);
            color: var(--accent);
        }

        .tree-toggle {
            width: 16px;
            height: 16px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 10px;
            color: var(--ink-tertiary);
            transition: transform 0.2s;
        }

        .tree-toggle.expanded {
            transform: rotate(90deg);
        }

        .tree-children {
            padding-left: 24px;
            display: none;
        }

        .tree-children.expanded {
            display: block;
        }

        .tree-icon {
            font-size: 14px;
            opacity: 0.7;
        }

        /* 标签 */
        .tags-cloud {
            display: flex;
            flex-wrap: wrap;
            gap: 6px;
            padding: 0 4px;
        }

        .tag {
            padding: 4px 10px;
            background: var(--accent-light);
            color: var(--accent);
            border-radius: 12px;
            font-size: 12px;
            font-weight: 500;
            cursor: pointer;
            transition: var(--transition);
        }

        .tag:hover {
            background: var(--accent);
            color: white;
        }

        /* 底部用户信息 */
        .sidebar-footer {
            padding: 16px;
            border-top: 1px solid var(--border);
            display: flex;
            align-items: center;
            gap: 12px;
        }

        .avatar {
            width: 36px;
            height: 36px;
            border-radius: 50%;
            background: var(--accent);
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-weight: 600;
            font-size: 14px;
        }

        .user-info {
            flex: 1;
        }

        .user-name {
            font-size: 14px;
            font-weight: 500;
            color: var(--ink-primary);
        }

        .user-plan {
            font-size: 12px;
            color: var(--accent);
        }

        /* ========== 中间内容区 ========== */
        .main-content {
            display: flex;
            flex-direction: column;
            overflow: hidden;
            background: var(--paper-bg);
        }

        .content-header {
            padding: 16px 32px;
            border-bottom: 1px solid var(--border);
            display: flex;
            align-items: center;
            justify-content: space-between;
            background: rgba(250,249,246,0.95);
            backdrop-filter: blur(10px);
        }

        .breadcrumbs {
            display: flex;
            align-items: center;
            gap: 8px;
            font-size: 13px;
            color: var(--ink-tertiary);
        }

        .breadcrumbs a {
            color: var(--ink-tertiary);
            text-decoration: none;
            transition: var(--transition);
        }

        .breadcrumbs a:hover {
            color: var(--accent);
        }

        .breadcrumbs .current {
            color: var(--ink-primary);
            font-weight: 500;
        }

        .header-actions {
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .btn-icon {
            width: 36px;
            height: 36px;
            border-radius: var(--radius);
            border: 1px solid var(--border);
            background: var(--card-bg);
            color: var(--ink-secondary);
            cursor: pointer;
            display: flex;
            align-items: center;
            justify-content: center;
            transition: var(--transition);
            font-size: 14px;
        }

        .btn-icon:hover {
            background: var(--sidebar-bg);
            color: var(--ink-primary);
        }

        .save-status {
            font-size: 12px;
            color: var(--success);
            display: flex;
            align-items: center;
            gap: 4px;
        }

        .content-body {
            flex: 1;
            overflow-y: auto;
            padding: 32px;
        }

        .content-body::-webkit-scrollbar {
            width: 6px;
        }

        .content-body::-webkit-scrollbar-track {
            background: transparent;
        }

        .content-body::-webkit-scrollbar-thumb {
            background: var(--border);
            border-radius: 3px;
        }

        /* 笔记内容样式 */
        .note-container {
            max-width: 720px;
            margin: 0 auto;
        }

        .note-title {
            font-size: 32px;
            font-weight: 700;
            color: var(--ink-primary);
            line-height: 1.3;
            margin-bottom: 12px;
            letter-spacing: -0.5px;
        }

        .note-meta {
            display: flex;
            align-items: center;
            gap: 16px;
            margin-bottom: 20px;
            font-size: 13px;
            color: var(--ink-tertiary);
        }

        .note-tags {
            display: flex;
            gap: 8px;
            margin-bottom: 28px;
        }

        .note-tag {
            padding: 4px 12px;
            background: var(--accent-light);
            color: var(--accent);
            border-radius: 12px;
            font-size: 12px;
            font-weight: 500;
            cursor: pointer;
            transition: var(--transition);
        }

        .note-tag:hover {
            background: var(--accent);
            color: white;
        }

        /* 内容块 */
        .content-block {
            margin-bottom: 24px;
        }

        .content-block h2 {
            font-size: 22px;
            font-weight: 600;
            color: var(--ink-primary);
            margin-bottom: 12px;
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .content-block h2::before {
            content: '';
            width: 4px;
            height: 24px;
            background: var(--accent);
            border-radius: 2px;
        }

        .content-block p {
            font-size: 16px;
            color: var(--ink-primary);
            line-height: 1.8;
            margin-bottom: 16px;
        }

        .content-block ul {
            list-style: none;
            padding-left: 4px;
        }

        .content-block ul li {
            position: relative;
            padding-left: 24px;
            margin-bottom: 10px;
            font-size: 15px;
            color: var(--ink-primary);
            line-height: 1.7;
        }

        .content-block ul li::before {
            content: '';
            position: absolute;
            left: 0;
            top: 10px;
            width: 8px;
            height: 8px;
            background: var(--accent);
            border-radius: 50%;
            opacity: 0.6;
        }

        /* 引用块 */
        .quote-block {
            background: var(--sidebar-bg);
            border-left: 4px solid var(--accent);
            padding: 20px 24px;
            border-radius: 0 var(--radius) var(--radius) 0;
            margin: 24px 0;
        }

        .quote-block p {
            font-style: italic;
            color: var(--ink-secondary);
            margin-bottom: 8px;
            font-size: 15px;
        }

        .quote-block cite {
            font-size: 13px;
            color: var(--ink-tertiary);
            font-style: normal;
        }

        /* 双向链接卡片 */
        .link-cards {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 12px;
            margin: 20px 0;
        }

        .link-card {
            background: var(--card-bg);
            border: 1px solid var(--border);
            border-radius: var(--radius);
            padding: 16px;
            cursor: pointer;
            transition: var(--transition);
        }

        .link-card:hover {
            border-color: var(--accent);
            box-shadow: var(--shadow-sm);
        }

        .link-card-title {
            font-size: 14px;
            color: var(--accent);
            font-weight: 500;
            margin-bottom: 6px;
        }

        .link-card-meta {
            font-size: 12px;
            color: var(--ink-tertiary);
        }

        /* 图片占位 */
        .image-placeholder {
            background: var(--sidebar-bg);
            border: 2px dashed var(--border);
            border-radius: var(--radius-lg);
            padding: 60px;
            text-align: center;
            margin: 24px 0;
        }

        .image-placeholder-icon {
            font-size: 40px;
            color: var(--ink-tertiary);
            margin-bottom: 12px;
        }

        .image-placeholder-text {
            font-size: 14px;
            color: var(--ink-tertiary);
        }

        /* 代码块 */
        .code-block {
            background: #1E1E1E;
            border-radius: var(--radius-lg);
            overflow: hidden;
            margin: 24px 0;
        }

        .code-header {
            display: flex;
            align-items: center;
            gap: 8px;
            padding: 12px 16px;
            background: #2D2D2D;
        }

        .code-dot {
            width: 12px;
            height: 12px;
            border-radius: 50%;
        }

        .code-dot.red { background: #FF5F56; }
        .code-dot.yellow { background: #FFBD2E; }
        .code-dot.green { background: #27C93F; }

        .code-lang {
            margin-left: auto;
            font-size: 12px;
            color: var(--ink-tertiary);
            font-family: 'JetBrains Mono', monospace;
        }

        .code-content {
            padding: 16px;
            overflow-x: auto;
        }

        .code-content pre {
            font-family: 'JetBrains Mono', monospace;
            font-size: 13px;
            line-height: 1.6;
            color: #D4D4D4;
            white-space: pre-wrap;
        }

        .code-content .keyword { color: #569CD6; }
        .code-content .string { color: #CE9178; }
        .code-content .function { color: #DCDCAA; }
        .code-content .comment { color: #6A9955; }

        /* 底部关联区 */
        .note-footer {
            margin-top: 40px;
            padding-top: 20px;
            border-top: 1px solid var(--border);
            display: flex;
            gap: 24px;
            font-size: 13px;
            color: var(--ink-tertiary);
        }

        .note-footer span {
            cursor: pointer;
            transition: var(--transition);
        }

        .note-footer span:hover {
            color: var(--accent);
        }

        /* ========== 右侧边栏 ========== */
        .right-panel {
            background: var(--sidebar-bg);
            border-left: 1px solid var(--border);
            display: flex;
            flex-direction: column;
            overflow: hidden;
        }

        .panel-section {
            padding: 20px;
            border-bottom: 1px solid var(--border);
        }

        .panel-section-title {
            font-size: 12px;
            font-weight: 600;
            color: var(--ink-tertiary);
            text-transform: uppercase;
            letter-spacing: 0.5px;
            margin-bottom: 12px;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .panel-section-title a {
            font-size: 11px;
            color: var(--accent);
            text-decoration: none;
            text-transform: none;
            letter-spacing: 0;
        }

        /* 属性 */
        .property-row {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 6px 0;
            font-size: 13px;
        }

        .property-key {
            color: var(--ink-tertiary);
        }

        .property-value {
            color: var(--ink-primary);
            font-weight: 500;
        }

        .property-value.status {
            display: flex;
            align-items: center;
            gap: 6px;
        }

        .status-dot {
            width: 8px;
            height: 8px;
            border-radius: 50%;
            background: var(--success);
        }

        /* 大纲 */
        .outline-list {
            list-style: none;
        }

        .outline-item {
            padding: 4px 0;
            font-size: 13px;
            color: var(--ink-secondary);
            cursor: pointer;
            transition: var(--transition);
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
        }

        .outline-item:hover {
            color: var(--accent);
        }

        .outline-item.level-1 {
            font-weight: 600;
            color: var(--ink-primary);
        }

        .outline-item.level-2 {
            padding-left: 12px;
        }

        .outline-item.level-3 {
            padding-left: 24px;
            font-size: 12px;
        }

        /* 图谱 */
        .graph-container {
            height: 180px;
            background: var(--card-bg);
            border-radius: var(--radius);
            border: 1px solid var(--border);
            position: relative;
            overflow: hidden;
        }

        .graph-svg {
            width: 100%;
            height: 100%;
        }

        /* 反向链接 */
        .backlink-item {
            display: flex;
            align-items: center;
            gap: 10px;
            padding: 10px;
            background: var(--card-bg);
            border-radius: var(--radius);
            margin-bottom: 8px;
            cursor: pointer;
            transition: var(--transition);
            border: 1px solid transparent;
        }

        .backlink-item:hover {
            border-color: var(--border);
            box-shadow: var(--shadow-sm);
        }

        .backlink-icon {
            font-size: 16px;
            color: var(--ink-tertiary);
        }

        .backlink-info {
            flex: 1;
            min-width: 0;
        }

        .backlink-title {
            font-size: 13px;
            color: var(--ink-primary);
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
        }

        .backlink-time {
            font-size: 11px;
            color: var(--ink-tertiary);
        }

              /* 快速捕获悬浮按钮 */
        .fab {
            position: fixed;
            bottom: 24px;
            right: 24px;
            width: 56px;
            height: 56px;
            border-radius: 50%;
            background: var(--accent);
            color: white;
            border: none;
            font-size: 24px;
            cursor: pointer;
            box-shadow: var(--shadow-lg);
            transition: var(--transition);
            display: flex;
            align-items: center;
            justify-content: center;
            z-index: 50;
        }

        .fab:hover {
            transform: scale(1.05);
            background: var(--accent-hover);
        }

        /* 选中浮动工具栏 */
        .floating-toolbar {
            position: absolute;
            display: none;
            background: var(--card-bg);
            border: 1px solid var(--border);
            border-radius: var(--radius);
            box-shadow: var(--shadow-md);
            padding: 6px;
            gap: 4px;
            z-index: 10;
        }

        .floating-toolbar.visible {
            display: flex;
        }

        .toolbar-btn {
            width: 32px;
            height: 32px;
            border: none;
            background: transparent;
            border-radius: 4px;
            cursor: pointer;
            font-size: 14px;
            color: var(--ink-secondary);
            transition: var(--transition);
        }

        .toolbar-btn:hover {
            background: var(--sidebar-bg);
            color: var(--ink-primary);
        }

        /* 响应式 */
        @media (max-width: 1200px) {
            .app-container {
                grid-template-columns: 260px 1fr;
            }
            .right-panel {
                display: none;
            }
            .rag-panel {
                right: 24px;
            }
        }

        @media (max-width: 768px) {
            .app-container {
                grid-template-columns: 1fr;
            }
            .sidebar {
                display: none;
            }
        }
    </style>
<base target="_blank">
</head>
<body>
    <div class="app-container">
        <!-- ========== 左侧边栏 ========== -->
        <aside class="sidebar">
            <div class="sidebar-header">
                <div class="logo">
                    <div class="logo-icon">K</div>
                    Knowledge Base
                </div>
                <div class="search-box">
                    <span class="search-icon">Q</span>
                    <input type="text" placeholder="Search notes, tags...">
                </div>
                <button class="quick-capture-btn">+ Quick Capture</button>
            </div>

            <div class="sidebar-content">
                <div class="sidebar-section">
                    <div class="sidebar-section-title">Workspace</div>
                    <a href="#" class="nav-item active">
                        <span>Inbox</span>
                        <span class="badge">12</span>
                    </a>
                    <a href="#" class="nav-item">
                        <span>Favorites</span>
                        <span class="badge">3</span>
                    </a>
                    <a href="#" class="nav-item">
                        <span>Recent Deleted</span>
                    </a>
                </div>

                <div class="sidebar-section">
                    <div class="sidebar-section-title">Catalog</div>
                    <div class="file-tree">
                        <div class="tree-item active">
                            <span class="tree-toggle expanded">▶</span>
                            <span class="tree-icon">📁</span>
                            <span>Project Management</span>
                        </div>
                        <div class="tree-children expanded">
                            <div class="tree-item">
                                <span class="tree-icon">📄</span>
                                <span>Requirements</span>
                            </div>
                            <div class="tree-item">
                                <span class="tree-icon">📄</span>
                                <span>Tech Specs</span>
                            </div>
                            <div class="tree-item">
                                <span class="tree-icon">📄</span>
                                <span>Meeting Notes</span>
                            </div>
                        </div>
                        <div class="tree-item">
                            <span class="tree-toggle">▶</span>
                            <span class="tree-icon">📁</span>
                            <span>Study Notes</span>
                        </div>
                        <div class="tree-item">
                            <span class="tree-toggle">▶</span>
                            <span class="tree-icon">📁</span>
                            <span>Life Journal</span>
                        </div>
                        <div class="tree-item">
                            <span class="tree-toggle">▶</span>
                            <span class="tree-icon">📁</span>
                            <span>Ideas</span>
                        </div>
                    </div>
                </div>

                <div class="sidebar-section">
                    <div class="sidebar-section-title">Tags</div>
                    <div class="tags-cloud">
                        <span class="tag">#FastAPI</span>
                        <span class="tag">#Vue3</span>
                        <span class="tag">#Design</span>
                        <span class="tag">#Product</span>
                        <span class="tag">#Reading</span>
                        <span class="tag">#Draft</span>
                    </div>
                </div>
            </div>

            <div class="sidebar-footer">
                <div class="avatar">K</div>
                <div class="user-info">
                    <div class="user-name">My Knowledge</div>
                    <div class="user-plan">Pro Member</div>
                </div>
            </div>
        </aside>

        <!-- ========== 中间内容区 ========== -->
        <main class="main-content">
            <header class="content-header">
                <div class="breadcrumbs">
                    <a href="#">Project Mgmt</a>
                    <span>></span>
                    <a href="#">Tech Specs</a>
                    <span>></span>
                    <span class="current">Knowledge Base UI Design</span>
                </div>
                <div class="header-actions">
                    <span class="save-status">Saved</span>
                    <button class="btn-icon">◀</button>
                    <button class="btn-icon">▶</button>
                    <button class="btn-icon">↻</button>
                    <button class="btn-icon">⋮</button>
                </div>
            </header>

            <div class="content-body">
                <article class="note-container">
                    <h1 class="note-title">Knowledge Base UI Design</h1>
                    <div class="note-meta">
                        <span>Created 2026-06-25</span>
                        <span>·</span>
                        <span>Edited 2m ago</span>
                        <span>·</span>
                        <span>1,234 words</span>
                    </div>
                    <div class="note-tags">
                        <span class="note-tag">#Design</span>
                        <span class="note-tag">#Product</span>
                    </div>

                    <div class="content-block">
                        <h2>Core Design Principles</h2>
                        <p>Knowledge base UI must balance clear organization with smooth creation flow. The interface should feel like an infinite canvas rather than a complex maze.</p>
                        <ul>
                            <li><strong>Progressive Disclosure:</strong> Layer features by context, avoid information overload</li>
                            <li><strong>Bidirectional Links:</strong> Visualize connections between notes naturally</li>
                            <li><strong>Low Friction Capture:</strong> Support quick idea capture with minimal steps</li>
                            <li><strong>Context Preservation:</strong> Maintain spatial awareness during reading and editing</li>
                        </ul>
                    </div>

                    <div class="quote-block">
                        <p>"Good knowledge base UI should feel like an infinite sheet of paper, not a complex maze."</p>
                        <cite>— From "How to Take Smart Notes"</cite>
                    </div>

                    <div class="content-block">
                        <h2>Related Concepts</h2>
                        <div class="link-cards">
                            <div class="link-card">
                                <div class="link-card-title">[[Zettelkasten Method]]</div>
                                <div class="link-card-meta">3 backlinks · Created 2026-06-20</div>
                            </div>
                            <div class="link-card">
                                <div class="link-card-title">[[Progressive Summarization]]</div>
                                <div class="link-card-meta">5 backlinks · Created 2026-06-18</div>
                            </div>
                        </div>
                    </div>

                    <div class="image-placeholder">
                        <div class="image-placeholder-icon">🖼️</div>
                        <div class="image-placeholder-text">Three-Column Layout Architecture</div>
                    </div>

                    <div class="code-block">
                        <div class="code-header">
                            <span class="code-dot red"></span>
                            <span class="code-dot yellow"></span>
                            <span class="code-dot green"></span>
                            <span class="code-lang">javascript</span>
                        </div>
                        <div class="code-content">
                            <pre><span class="comment">// Create bidirectional link</span>
<span class="keyword">const</span> <span class="function">link</span> = <span class="keyword">await</span> <span class="function">createLink</span>({
  <span class="string">source</span>: <span class="string">"current-note-id"</span>,
  <span class="string">target</span>: <span class="string">"target-note-id"</span>,
  <span class="string">type</span>: <span class="string">"reference"</span>
});</pre>
                        </div>
                    </div>

                    <div class="note-footer">
                        <span>← Backlinks (8)</span>
                        <span>→ Outgoing (12)</span>
                        <span>* Related (5)</span>
                    </div>
                </article>
            </div>
        </main>

        <!-- ========== 右侧边栏 ========== -->
        <aside class="right-panel">
            <div class="panel-section">
                <div class="panel-section-title">Properties</div>
                <div class="property-row">
                    <span class="property-key">Status</span>
                    <span class="property-value status">
                        <span class="status-dot"></span>
                        Ongoing
                    </span>
                </div>
                <div class="property-row">
                    <span class="property-key">Priority</span>
                    <span class="property-value">High</span>
                </div>
                <div class="property-row">
                    <span class="property-key">Project</span>
                    <span class="property-value">KB Refactor</span>
                </div>
                <div class="property-row">
                    <span class="property-key">Owner</span>
                    <span class="property-value">Me</span>
                </div>
            </div>

            <div class="panel-section">
                <div class="panel-section-title">Outline</div>
                <ul class="outline-list">
                    <li class="outline-item level-1">Core Design Principles</li>
                    <li class="outline-item level-2">Layout Architecture</li>
                    <li class="outline-item level-2">Interaction Details</li>
                    <li class="outline-item level-3">Bidirectional Links</li>
                    <li class="outline-item level-3">Graph View</li>
                    <li class="outline-item level-2">Visual Style</li>
                </ul>
            </div>

            <div class="panel-section">
                <div class="panel-section-title">
                    Local Graph
                    <a href="#">View All →</a>
                </div>
                <div class="graph-container">
                    <svg class="graph-svg" viewBox="0 0 280 180">
                        <!-- 连线 -->
                        <line x1="140" y1="90" x2="200" y2="50" stroke="#D1D1D6" stroke-width="1.5"/>
                        <line x1="140" y1="90" x2="220" y2="100" stroke="#D1D1D6" stroke-width="1.5"/>
                        <line x1="140" y1="90" x2="190" y2="140" stroke="#D1D1D6" stroke-width="1.5"/>
                        <line x1="140" y1="90" x2="90" y2="140" stroke="#D1D1D6" stroke-width="1.5"/>
                        <line x1="140" y1="90" x2="70" y2="80" stroke="#D1D1D6" stroke-width="1.5"/>
                        <line x1="140" y1="90" x2="80" y2="40" stroke="#D1D1D6" stroke-width="1.5"/>

                        <!-- 中心节点 -->
                        <circle cx="140" cy="90" r="22" fill="#2563EB" opacity="0.9"/>
                        <text x="140" y="94" text-anchor="middle" fill="white" font-size="10" font-weight="bold">NOW</text>

                        <!-- 周围节点 -->
                        <circle cx="200" cy="50" r="16" fill="#5856D6" opacity="0.8"/>
                        <text x="200" y="54" text-anchor="middle" fill="white" font-size="8" font-weight="bold">Zett</text>

                        <circle cx="220" cy="100" r="16" fill="#FF9500" opacity="0.8"/>
                        <text x="220" y="104" text-anchor="middle" fill="white" font-size="8" font-weight="bold">Des</text>

                        <circle cx="190" cy="140" r="16" fill="#34C759" opacity="0.8"/>
                        <text x="190" y="144" text-anchor="middle" fill="white" font-size="8" font-weight="bold">Vue</text>

                        <circle cx="90" cy="140" r="16" fill="#FF3B30" opacity="0.8"/>
                        <text x="90" y="144" text-anchor="middle" fill="white" font-size="8" font-weight="bold">Pro</text>

                        <circle cx="70" cy="80" r="16" fill="#AF52DE" opacity="0.8"/>
                        <text x="70" y="84" text-anchor="middle" fill="white" font-size="8" font-weight="bold">Read</text>

                        <circle cx="80" cy="40" r="16" fill="#FF2D55" opacity="0.8"/>
                        <text x="80" y="44" text-anchor="middle" fill="white" font-size="8" font-weight="bold">Meth</text>
                    </svg>
                </div>
            </div>

            <div class="panel-section">
                <div class="panel-section-title">Backlinks</div>
                <div class="backlink-item">
                    <span class="backlink-icon">📄</span>
                    <div class="backlink-info">
                        <div class="backlink-title">How to Build Second Brain</div>
                        <div class="backlink-time">2d ago</div>
                    </div>
                </div>
                <div class="backlink-item">
                    <span class="backlink-icon">📄</span>
                    <div class="backlink-info">
                        <div class="backlink-title">Note Tools Comparison</div>
                        <div class="backlink-time">1w ago</div>
                    </div>
                </div>
                <div class="backlink-item">
                    <span class="backlink-icon">📄</span>
                    <div class="backlink-info">
                        <div class="backlink-title">2026 Learning Plan</div>
                        <div class="backlink-time">2w ago</div>
                    </div>
                </div>
            </div>

            <div class="panel-section">
                <div class="panel-section-title">AI Assistant</div>
                <div style="font-size: 13px; color: var(--ink-secondary); line-height: 1.8;">
                    <div style="padding: 8px; border-radius: var(--radius); cursor: pointer; transition: var(--transition);" onmouseover="this.style.background='var(--hover-bg)'" onmouseout="this.style.background='transparent'">
                        💡 Summarize key points
                    </div>
                    <div style="padding: 8px; border-radius: var(--radius); cursor: pointer; transition: var(--transition);" onmouseover="this.style.background='var(--hover-bg)'" onmouseout="this.style.background='transparent'">
                        🗺️ Generate mind map
                    </div>
                    <div style="padding: 8px; border-radius: var(--radius); cursor: pointer; transition: var(--transition);" onmouseover="this.style.background='var(--hover-bg)'" onmouseout="this.style.background='transparent'">
                        🔍 Find related notes
                    </div>
                </div>
            </div>
        </aside>
    </div>

   
    <!-- 快速捕获悬浮按钮 -->
    <button class="fab">+</button>

    <!-- 选中浮动工具栏 -->
    <div class="floating-toolbar" id="floatingToolbar">
        <button class="toolbar-btn">B</button>
        <button class="toolbar-btn">I</button>
        <button class="toolbar-btn">H</button>
        <button class="toolbar-btn">🔗</button>
        <button class="toolbar-btn">「」</button>
        <button class="toolbar-btn">⋯</button>
    </div>

    <script>
     
        // 文件树展开/折叠
        document.querySelectorAll('.tree-toggle').forEach(toggle => {
            toggle.addEventListener('click', (e) => {
                e.stopPropagation();
                const item = toggle.closest('.tree-item');
                const children = item.nextElementSibling;
                if (children && children.classList.contains('tree-children')) {
                    toggle.classList.toggle('expanded');
                    children.classList.toggle('expanded');
                }
            });
        });

        // 模拟选中文字显示工具栏
        document.addEventListener('mouseup', () => {
            const selection = window.getSelection();
            const toolbar = document.getElementById('floatingToolbar');
            if (selection.toString().length > 0) {
                const range = selection.getRangeAt(0);
                const rect = range.getBoundingClientRect();
                toolbar.style.left = rect.left + rect.width / 2 - 100 + 'px';
                toolbar.style.top = rect.top - 50 + 'px';
                toolbar.classList.add('visible');
            } else {
                toolbar.classList.remove('visible');
            }
        });
    </script>
</body>
</html>